# Frontend

Dark-mode Next.js dashboard for the stock alpha project.

## Pages

- `Overview`
- `Metrics`
- `Inference`
- `Screener`

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` if your FastAPI backend is not running on `http://localhost:8000`.
